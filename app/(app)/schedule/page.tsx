'use client'
// app/(app)/schedule/page.tsx
// Schedule view — placeholder while dynamic recurring schedule is built.

export default function SchedulePage() {
  return (
    <div className="schedule-empty">
      <div className="schedule-empty-icon">📅</div>
      <div className="schedule-empty-title">Schedule</div>
      <div className="schedule-empty-sub">
        Recurring routines and weekly rhythms will live here.
        <br />
        Coming soon — for now, use tasks to track what needs doing.
      </div>
    </div>
  )
}
